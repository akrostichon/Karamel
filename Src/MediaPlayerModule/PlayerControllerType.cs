namespace MediaPlayer
{
    /// <summary>
    /// enum for the player controller type (is the song that is played a video or an mp3+g)
    /// </summary>
    internal enum PlayerControllerType
    {
        Mp3G = 1,
        Video = 2
    }
}
